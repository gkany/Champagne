{    
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            // "command": "cmake -DCMAKE_BUILD_TYPE=Debug & make",
            "command": "make -j2",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
